import React from "react";

const SVGIcon = ({icon}) => {
  console.log(icon)
	return (
    <p>Hola</p>
		// <div dangerouslySetInnerHTML={{ __html: `../assets/${icon.icon}.svg` }} />
  )
}
export default SVGIcon;
